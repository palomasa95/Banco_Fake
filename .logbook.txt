#### LOGBOOK DO PROJETO 'BANCO_FAKE' ####
-----------------------------------------
Autor: Paloma Sá.
Date: Seg 08 Dez 2025 02:40:00 -03 (Data da Última Revisão)
Objetivo: Aprender e fixar conceitos e técnicas de análise de dados, Big Data, e segurança
a partir de dados fictícios gerados no Python.

Habilidades alvo:
* Git (Versionamento, Merge, Branches)
* SQL (Consultas, Otimização, Indexação)
* Comandos de Terminal (awk, grep, pipe, redirecionamento)
* Data Wrangling e Limpeza de Dados
* Segurança e Documentação (Log, Sandbox)
------------------------------------------
#### INÍCIO DOS LOGS ####

## 2025-12-07: Configuração e Geração de Dados Mestre

* **Ação:** Instalação da biblioteca **Faker** no ambiente virtual (`pip install Faker`).
* **Ação:** Criação e execução do script `gerar_clientes.py`.
* **Resultado:** Gerados **12.000 clientes** em `CLIENTES.csv`.
* **Ação:** Criação da pasta `banco_fake` e **renomeação/movimentação** de `CLIENTES.csv` para `banco_fake/clientes.csv` (Ajustando padrão de nomenclatura para minúsculas).
* **Ação:** Criação do script `gerar_contas.py`.
* **Problema Resolvido:** Corrigido erro de sintaxe Python (`SyntaxError: name 'CONTAS_GERADAS' is assigned to before global declaration`).
* **Resultado:** Geradas **19.991 contas** em `banco_fake/CONTAS.csv`.

## 2025-12-08: Geração de Risco, Big Data e Carga SQL

* **Ação:** Criação do script `gerar_credito.py`.
* **Problema Resolvido (1):** Corrigido erro de sintaxe Python (`SyntaxError` na declaração `global CREDITO_GERADO`).
* **Problema Resolvido (2):** Corrigido erro de *range* do Faker (`ValueError: empty range for _rand_seconds`) causado por regras de data de aprovação incompatíveis com a data de nascimento do cliente (Regra de 18.5 anos).
* **Resultado:** Geradas **9.531 operações de crédito** em `banco_fake/CREDITO.csv`.
* **Ação:** Criação e execução do script de teste `teste_gerar_transacoes.py` (1000 registros).
* **Ação Crítica:** Execução do script **`gerar_transacoes.py`**.
    * **Resultado:** Gerados **10.000.000 transações** usando a técnica de *chunking* (escrita em blocos de 100k) para gerenciar a RAM.
* **Ação:** **Renomeação final dos arquivos** para minúsculas e padrão SQL (ex: `CONTAS.csv` para `contas.csv`) dentro da pasta `banco_fake`.
* **Ação:** Instalação do SQLite (`sudo apt install sqlite3`).
* **Ação:** Importação de todos os CSVs para o banco `banco_fake.db` com sucesso.
* **Verificação:** Contagem confirmada de **10.000.000** registros na tabela `transacoes` via `SELECT COUNT(*)`.

* **Ação:** git init no diretório ~/Scripts/Dados$
* **Ação:** Renomeei o antido diretório 'Dados' para 'Banco_Fake' (~/Scripts/Banco_Fake$) para garantir a organização futura e escalabilidade do projeto.
* **Ação:** Modifiquei a estrutura de diretórios para organizar melhor o projeto.( criados: scripts data analysis data/banco_fake data/backups )
* **Ação:** Para evitar modificação acidental e melhorar a organização renomeei o arquivo logbook.txt para .logbook.txt para estar oculto no diretório, uma vez que manuseio com echo incremental >>

* **Status:** Estrutura atual do ~/Scripts/Banco_Fake$: .
├── analysis
├── data
│   ├── backups
│   ├── banco_fake
│   │   ├── clientes.csv
│   │   ├── contas.csv
│   │   ├── credito.csv
│   │   ├── transacoes.csv
│   │   └── transacoes_teste.csv
│   └── banco_fake.db
├── .logbook.txt
└── scripts
    ├── 01_gerar_clientes.py
    ├── 02_gerar_contas.py
    ├── 03_gerar_credito.py
    ├── 04_gerar_transacoes.py
    └── teste_gerar_transacoes.py

6 directories, 12 files
* **Ação:** Primeiro commit Git (Hash: de13274) realizado com sucesso. Estrutura finalizada, scripts e logbook versionados no branch 'main'. Fim da rotina de segurança diária.
